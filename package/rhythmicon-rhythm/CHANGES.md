# 0.5.0

- Extend `toString`
- Extend documentation
- Change Tracy numbers back to its original form

# 0.4.1

- Change Tracy numbers to reversed Tracy numbers
- Remove method `fromHex` (needs discussion)
- Rename `toDurationString` to `toDurations`

# 0.4.0

- Make `0` a rest value as well
- Add factory methods `fromTracy` and `fromHex`
- Rename factory method `euclidean` to `fromEuclidean`
- Add methods `includes` and `condense`

# 0.3.0

- Disallow `|...|` pattern strings
- Add method `includes`
- Rename method `copy` to `clone`
- Rename method `isShuffle` to `shuffled`
- Rename method `rotation` to `rotated`
- Rewrite README

# 0.2.1

- Add method `odd` to check *rhythmic oddity*

