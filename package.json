{
  "name": "rhythmicon",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint --ext .js,.vue .",
    "fix": "eslint --fix --ext .js,.vue .",
    "test": "npm test --if-present --workspaces",
    "build": "npm run rhythms; npm run build --if-present --workspaces",
    "rhythms": "node bin/build.js"
  },
  "devDependencies": {
    "@eslint/compat": "^2.0.0",
    "@vitest/coverage-v8": "^4.0.18",
    "@vue/test-utils": "^2.4.6",
    "c8": "^12.0.0",
    "eslint": "^10.0.0",
    "eslint-config-gbv": "^2.6.0",
    "eslint-plugin-vue": "^10.0.0",
    "js-yaml": "^5.0.1"
  },
  "workspaces": [
    "package/*"
  ],
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
